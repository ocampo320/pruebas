class Vehicle {
  String transportType;
  String vehicleIdentification;
  String driver;
  String driverIdentification;
  double totalCapacity;

  Vehicle(this.transportType, this.vehicleIdentification, this.driver,
      this.driverIdentification, this.totalCapacity);
}
